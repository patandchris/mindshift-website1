[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA catch-all redirect — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
